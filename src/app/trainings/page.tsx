'use client';

import React, { useEffect, useMemo, useState } from 'react';
import { useRouter } from 'next/navigation';
import { AlertTriangle, Award, BookOpen, CheckCircle2, Clock3, Search } from 'lucide-react';
import { StorageAPI, initLocalStorage } from '@/lib/storage';
import { Training, UserProfile } from '@/types';
import { isTrainingAvailable } from '@/lib/utils';
import LontarLoadingSpinner from '@/components/LontarLoadingSpinner';

type Filter = 'all' | 'ongoing' | 'completed';
type TrainingView = { training: Training; status: 'not_started' | 'ongoing' | 'completed'; score: number | null; certificate: boolean; materialsCompleted: number; totalMaterials: number; daysLeft: number | null; };

function daysUntilEnd(endDate?: string): number | null { if (!endDate) return null; const diff = new Date(endDate).getTime() - Date.now(); return Math.max(0, Math.ceil(diff / 86400000)); }

export default function TrainingsPage() {
  const router = useRouter();
  const [user, setUser] = useState<UserProfile | null>(null);
  const [items, setItems] = useState<TrainingView[]>([]);
  const [loading, setLoading] = useState(true);
  const [filter, setFilter] = useState<Filter>('all');
  const [query, setQuery] = useState('');
  useEffect(() => { const load = async () => { try { await initLocalStorage(); const currentUser=StorageAPI.getCurrentUser(); if(!currentUser)return void router.push('/login'); if(currentUser.role==='admin')return void router.push('/admin'); setUser(currentUser); const trainings=StorageAPI.getTrainings().filter(t=>isTrainingAvailable(t)); const view:TrainingView[]=[]; for(const training of trainings){ await StorageAPI.loadTrainingResources(training.id); const pre=StorageAPI.getTestAttempts(currentUser.id,'pretest',training.id); const post=StorageAPI.getTestAttempts(currentUser.id,'posttest',training.id); const materials=StorageAPI.getMaterials(training.id).filter(m=>m.active); const materialIds=new Set(materials.map(m=>m.id)); const progress=StorageAPI.getMaterialProgress(currentUser.id).filter(row=>materialIds.has(row.material_id)); const completedIds=new Set(progress.filter(row=>row.completed_at).map(row=>row.material_id)); const passed=post.some(a=>a.score>=training.passing_score); const cert=Boolean(StorageAPI.getCertificateForUser(currentUser.id,training.id)); const bestScore=post.length?Math.max(...post.map(a=>a.score)):null; const hasProgress=pre.length>0||post.length>0||progress.length>0; view.push({training,status:cert||passed?'completed':hasProgress?'ongoing':'not_started',score:bestScore,certificate:cert,materialsCompleted:completedIds.size,totalMaterials:materials.length,daysLeft:daysUntilEnd(training.end_date)}); } setItems(view); } finally { setLoading(false); } }; void load(); },[router]);
  const filtered=useMemo(()=>items.filter(item=>{const matches=filter==='all'||(filter==='ongoing'?item.status==='ongoing':item.status==='completed'); return matches&&`${item.training.title} ${item.training.description||''}`.toLowerCase().includes(query.trim().toLowerCase());}),[items,filter,query]);
  const openTraining=async(training:Training)=>{StorageAPI.setSelectTraining(training.id);await StorageAPI.loadTrainingResources(training.id);router.push('/dashboard');};
  if(loading||!user)return <div className="mx-auto max-w-md py-16 text-center"><LontarLoadingSpinner size="lg" text="Memuat pelatihan Anda..." /></div>;
  const completedCount=items.filter(i=>i.status==='completed').length; const ongoingCount=items.filter(i=>i.status==='ongoing').length;
  return <div className="mx-auto max-w-6xl space-y-5 py-2"><section className="overflow-hidden rounded-3xl bg-[#07375c] p-6 text-white shadow-sm sm:p-8"><div className="max-w-2xl"><span className="text-xs font-bold uppercase tracking-[0.2em] text-sky-200">Daftar Pelatihan</span><h1 className="mt-2 text-2xl font-bold sm:text-3xl">Pelatihan Saya</h1><p className="mt-2 text-sm text-sky-100/80">Lanjutkan pembelajaran dari progres terakhir dan selesaikan sebelum periode pelatihan berakhir.</p></div><div className="mt-6 grid max-w-xl grid-cols-3 gap-2"><div className="rounded-lg bg-white/10 p-3"><b className="block text-2xl">{items.length}</b><span className="text-xs">Tersedia</span></div><div className="rounded-lg bg-white/10 p-3"><b className="block text-2xl">{ongoingCount}</b><span className="text-xs">Berjalan</span></div><div className="rounded-lg bg-white/10 p-3"><b className="block text-2xl">{completedCount}</b><span className="text-xs">Selesai</span></div></div></section><section className="rounded-2xl border border-slate-200 bg-white p-4 dark:border-slate-800 dark:bg-slate-900"><div className="flex flex-col gap-3 sm:flex-row sm:justify-between"><div className="flex gap-2 overflow-x-auto">{([['all','Semua'],['ongoing','Berjalan'],['completed','Selesai']] as [Filter,string][]).map(([v,l])=><button key={v} onClick={()=>setFilter(v)} className={`min-h-11 rounded-lg px-4 text-xs font-bold ${filter===v?'bg-[#07375c] text-white':'bg-slate-100 text-slate-600 dark:bg-slate-800 dark:text-slate-300'}`}>{l}</button>)}</div><label className="flex items-center gap-2 rounded-xl border px-3 py-2"><Search className="h-4 w-4 text-slate-400"/><input aria-label="Cari pelatihan" value={query} onChange={e=>setQuery(e.target.value)} className="w-full bg-transparent text-xs outline-none sm:w-56" placeholder="Cari pelatihan..."/></label></div></section>{filtered.length?<div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{filtered.map(({training,status,score,certificate,materialsCompleted,totalMaterials,daysLeft})=>{const completed=status==='completed',ongoing=status==='ongoing',urgent=!completed&&daysLeft!==null&&daysLeft<=3;return <article key={training.id} className="flex min-h-[270px] flex-col rounded-2xl border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><div className="flex justify-between gap-3"><div className={`flex h-11 w-11 items-center justify-center rounded-xl ${completed?'bg-emerald-100 text-emerald-700':'bg-[#07375c]/10 text-[#07375c]'}`}>{completed?<CheckCircle2 className="h-5 w-5"/>:<BookOpen className="h-5 w-5"/>}</div><span className={`h-fit rounded-full px-2.5 py-1 text-xs font-bold ${completed?'bg-emerald-100 text-emerald-700':ongoing?'bg-blue-100 text-blue-700':'bg-slate-100 text-slate-600'}`}>{completed?'Selesai':ongoing?'Sedang Berjalan':'Belum Dimulai'}</span></div><h2 className="mt-4 line-clamp-2 text-base font-bold">{training.title}</h2><p className="mt-2 line-clamp-2 text-xs text-slate-500">{training.description||'Pelatihan online LONTAR RSUD Prof. Dr. W.Z. Johannes Kupang.'}</p>{urgent&&<div className="mt-3 flex items-center gap-2 rounded-lg bg-amber-50 px-3 py-2 text-xs font-semibold text-amber-800 dark:bg-amber-950/30 dark:text-amber-300"><AlertTriangle className="h-4 w-4"/>{daysLeft===0?'Periode pelatihan berakhir hari ini.':`Sisa ${daysLeft} hari sebelum pelatihan berakhir.`}</div>}<div className="mt-auto pt-5"><div className="mb-3 flex flex-wrap gap-2 text-xs text-slate-500"><span className="inline-flex items-center gap-1 rounded-lg bg-slate-50 px-2 py-1 dark:bg-slate-800"><Clock3 className="h-3 w-3"/>Materi {materialsCompleted}/{totalMaterials}</span>{score!==null&&<span className="rounded-lg bg-slate-50 px-2 py-1 dark:bg-slate-800">Nilai terbaik: <b>{score}</b></span>}{certificate&&<span className="inline-flex items-center gap-1 rounded-lg bg-amber-50 px-2 py-1 text-amber-700"><Award className="h-3 w-3"/> Sertifikat tersedia</span>}</div><button onClick={()=>void openTraining(training)} className="w-full rounded-xl bg-[#07375c] px-4 py-3 text-xs font-bold text-white dark:bg-sky-400 dark:text-slate-950">{completed?'Lihat Pelatihan':ongoing?'Lanjutkan dari Progres Terakhir':'Mulai Pelatihan'}</button></div></article>})}</div>:<div className="rounded-2xl border border-dashed p-10 text-center"><BookOpen className="mx-auto h-9 w-9 text-slate-300"/><p className="mt-3 text-sm font-bold">Tidak ada pelatihan pada filter ini</p></div>}</div>;
}
